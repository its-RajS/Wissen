<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Wissen Account Activation</title>
    <style>
      body {
        background: #f4f4f7;
        font-family: "Segoe UI", Arial, sans-serif;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 480px;
        margin: 40px auto;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        padding: 32px 24px;
      }
      .header {
        text-align: center;
        margin-bottom: 24px;
      }
      .header h1 {
        color: #2d3748;
        font-size: 28px;
        margin: 0;
      }
      .greeting {
        font-size: 18px;
        color: #333;
        margin-bottom: 16px;
      }
      .message {
        font-size: 16px;
        color: #555;
        margin-bottom: 24px;
      }
      .activation-code-box {
        background: #f0f5ff;
        border: 1px dashed #3182ce;
        border-radius: 8px;
        padding: 18px 0;
        text-align: center;
        margin-bottom: 24px;
      }
      .activation-code {
        font-size: 32px;
        letter-spacing: 6px;
        color: #2b6cb0;
        font-weight: bold;
      }
      .footer {
        font-size: 14px;
        color: #888;
        text-align: center;
        margin-top: 32px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Activate Your Wissen Account</h1>
      </div>
      <div class="greeting">Hello <%= user.name %>,</div>
      <div class="message">
        Thank you for registering! Please use the activation code below to
        verify your account.
      </div>
      <div class="activation-code-box">
        <span class="activation-code"><%= activationCode %></span>
      </div>
      <div class="message">
        If you did not request this, you can safely ignore this email.
      </div>
      <div class="footer">
        &copy; <%= new Date().getFullYear() %> Your Company. All rights
        reserved.
      </div>
    </div>
  </body>
</html>
